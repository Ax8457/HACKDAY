<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Appointment Calendar</title>
    <link rel="stylesheet" href="/static/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        .sidebar-footer {
            margin-top: auto;
            padding: 20px;
            font-size: 0.75rem;
            color: #94a3b8;
            border-top: 1px solid #334155;
        }
    </style>
</head>
<body>

    <nav class="sidebar">
        <div class="sidebar-header">
            <span class="logo">PP | Planner Pro</span>
        </div>
        
        <div class="user-info">
            <p>Welcome, <strong>{{ username }}</strong></p>
        </div>

        <ul class="nav-links">
            <li><a href="/" class="active">Dashboard</a></li>
            <li><a href="/account">Account</a></li>
            <li class="divider"></li>
            <li><a href="/logout" class="logout-btn">Logout</a></li>
        </ul>

        <div class="sidebar-footer">
            <p>&copy; Hackday 2026 - Chall by 0x4X3L</p>
            <p>Front-end made by Gemini 3</p>
        </div>
    </nav>

    <main class="content">
        <header class="top-bar">
            <h1>Today is {{ weekday }} {{ day }} {{ month }}</h1>
        </header>

        <section class="controls">
            <form method="post" action="/" class="month-form">
                <label for="choosenmonth">View Month:</label>
                <select name="choosenmonth" id="choosenmonth" class="select-input">
                    <option value="1">January</option>
                    <option value="2">February</option>
                    <option value="3">March</option>
                    <option value="4">April</option>
                    <option value="5">May</option>
                    <option value="6">June</option>
                    <option value="7">July</option>
                    <option value="8">August</option>
                    <option value="9">September</option>
                    <option value="10">October</option>
                    <option value="11">November</option>
                    <option value="12">December</option>
                </select>
                <button type="submit" class="btn-primary">Go</button>
            </form>
            
            {% if selectedmonth %}
                <span class="badge">Viewing: {{ selectedmonth }}</span>
            {% endif %}
        </section>

        <div class="calendar-grid">
            {% for d in days %}
                {% set date_target = selectedmonth ~ d.0 ~ "#" %}
                
                {% set_global is_booked = false %}
                {% set_global appointment_count = 0 %}

                {% for ref in booked_refs %}
                    {% if ref is containing(date_target) %}
                        {% set_global is_booked = true %}
                        {% set_global appointment_count = appointment_count + 1 %}
                    {% endif %}
                {% endfor %}

                <div class="calendar-card {% if is_booked %} booked-disabled {% endif %} {% if d.0 == day and selectedmonth == month %} today {% endif %}"
                     {% if not is_booked %}
                        onclick="location.href='/book?d={{ d.0 }}&m={{ selectedmonth }}'"
                     {% endif %}>

                    <span class="card-weekday">{{ d.1 }}</span>
                    <span class="card-number">{{ d.0 }}</span>

                    <div class="card-footer">
                        {% if is_booked %}
                            <span class="status-hint">Reserved</span>
                            <div class="appointment-dots">
                                {% for i in range(end=appointment_count) %}
                                    <span class="dot"></span>
                                {% endfor %}
                            </div>
                        {% else %}
                            <span class="tap-hint">Book</span>
                        {% endif %}
                    </div>
                </div>
            {% endfor %}
        </div>
    </main>

</body>
</html>
